# ---- basic

alias c="clear"
alias l="ls -la"
alias o="cd .."
alias e="nvim"
alias zr="exec $SHELL -l"                    # reload shell

# --- yarn

alias yb="yarn build"
alias yt="yarn test"
alias ys="yarn serve"

# ---- helpers

alias pg="pdfgrep -rin"                      # search PDFs
alias cpwd="pwd | tr -d '\n' | pbcopy"       # copy pwd to clipboard

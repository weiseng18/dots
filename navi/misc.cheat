% misc

# Check if a LaTeX package is installed
kpsewhich <package-name>.sty

# Install LaTeX package
sudo tlmgr install <package-name>

# Generate code dependency graph
madge --image graph.png <file-name>

$file-name: ls

# Find files containing a search string, and open all in neovim tabs
nvim -p $(rg -l <search-string>)

# Zip a folder
zip -r <folder-name>.zip <folder-name>

$folder-name: ls
